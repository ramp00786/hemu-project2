# Runtime deps for the Voice RAG app (FastAPI + retrieve + Groq/Sarvam).
# Install from this folder:  pip install -r requirements.txt
#
# Offline-only extras:
#   one-time/download_dataset/requirements.txt   (datasets, pyarrow)
#   one-time/create_vector_database/requirements.txt  (already covered below)

fastapi>=0.112.0
uvicorn>=0.30.0
python-multipart>=0.0.9
python-dotenv>=1.0.1
httpx>=0.27.0
sentence-transformers>=3.0.0
faiss-cpu>=1.8.0
numpy>=1.26.0
